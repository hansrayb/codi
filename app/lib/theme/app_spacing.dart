/// Spacing scale Emas Berlian Insight — sistem 4px base.
///
/// Sumber kebenaran: `docs/03-DESIGN-SYSTEM.md`. Jangan pakai nilai
/// padding/margin di luar token ini.
abstract final class AppSpacing {
  const AppSpacing._();

  /// 2px — hairline gap.
  static const s2 = 2.0;

  /// 4px — tight gap.
  static const s4 = 4.0;

  /// 6px — small gap inline.
  static const s6 = 6.0;

  /// 8px — default gap small.
  static const s8 = 8.0;

  /// 12px — default gap.
  static const s12 = 12.0;

  /// 14px — card padding.
  static const s14 = 14.0;

  /// 16px — section padding.
  static const s16 = 16.0;

  /// 20px — screen padding horizontal.
  static const s20 = 20.0;

  /// 24px — large gap.
  static const s24 = 24.0;

  /// 32px — section separator.
  static const s32 = 32.0;

  /// 40px — hero spacing.
  static const s40 = 40.0;

  /// 48px — page separator.
  static const s48 = 48.0;
}
