/// Border radius token Emas Berlian Insight.
///
/// Sumber kebenaran: `docs/03-DESIGN-SYSTEM.md`.
abstract final class AppRadius {
  const AppRadius._();

  /// 4px — tag, small badge.
  static const r4 = 4.0;

  /// 8px — inline element.
  static const r8 = 8.0;

  /// 10px — chip, suggestion.
  static const r10 = 10.0;

  /// 12px — default button, period selector.
  static const r12 = 12.0;

  /// 14px — card item.
  static const r14 = 14.0;

  /// 16px — chart card.
  static const r16 = 16.0;

  /// 18px — chat message.
  static const r18 = 18.0;

  /// 20px — hero summary card.
  static const r20 = 20.0;

  /// 28px — logo container.
  static const r28 = 28.0;

  /// 999px — pill button, chip.
  static const rPill = 999.0;
}
